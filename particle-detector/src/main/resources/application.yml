spring:
  cloud:
    stream:
      bindings:
        event_store_input:
          group: ${KAFKA_GROUP}
          binder: kafka
          destination: events
        event_store_output:
          binder: kafka
          destination: events

      kafka:
        binder:
          headers:
            - AGGREGATE
            - EVENT_TYPE