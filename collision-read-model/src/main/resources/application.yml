spring:
  application:
    name: collision-read-model
  cloud:
    stream:
      bindings:
        event_store_input:
          group: ${KAFKA_GROUP}
          binder: kafka
          destination: events

      kafka:
        binder:
          headers:
            - AGGREGATE
            - EVENT_TYPE